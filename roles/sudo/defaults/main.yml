---
sudo_rules_dir: /etc/sudoers.d
sudo_visudo_path: /usr/sbin/visudo

# Set value (e.g. 60) to manage sudo timeout; null disables.
sudo_timestamp_timeout: null
sudo_timeout_rule_path: /etc/sudoers.d/timeout

sudo_rules_cleanup: false  # supprime les fichiers non gérés dans sudoers.d

sudo_rules: []
# Exemples:
# sudo_rules:
#   - name: deploy_user
#     subject: "deploy"          # user ou %groupe
#     nopasswd: true
#     commands:
#       - ALL
#
#   - name: ops_group
#     subject: "%ops"
#     nopasswd: false
#     commands:
#       - /usr/bin/systemctl
#       - /usr/bin/apt
#
#   - name: mixed_rule
#     subject: "bob"
#     commands:
#       - /usr/bin/visudo
#     nopasswd_commands:
#       - /usr/bin/systemctl
#       - /usr/bin/apt
